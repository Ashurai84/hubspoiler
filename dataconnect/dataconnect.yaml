specVersion: "v1beta"
serviceId: "new hack"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "new hack-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
